# Runtime interfaces

This directory contains user-facing and machine-facing interfaces. REST is the initial canonical external API; CLI, web and OLED interfaces should consume the same service contracts rather than duplicate business logic.
